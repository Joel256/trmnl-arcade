name = "joel-arcade"
main = "src/index.js"
compatibility_date = "2026-08-13"

# Cloudflare creates and owns the DNS record for a custom domain — never point a
# record at workers.dev by hand, it will not route.

[[kv_namespaces]]
binding = "ARCADE"
id = "fb7d5eeb69e542bc83b93a71a544bba9"
